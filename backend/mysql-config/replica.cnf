[mysqld]
# Server identification (will be overridden by docker-compose for each replica)
server-id = 2

# Performance schema
performance_schema = ON

# General settings
max_connections = 200

# Read-only mode (can be disabled during promotion to master)
# read_only = ON
